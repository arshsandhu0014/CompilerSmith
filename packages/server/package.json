{
  "name": "@compilersmith/server",
  "version": "1.0.0",
  "main": "./dist/server.js",
  "license": "MIT",
  "scripts": {
    "start": "tsc --build && node ./dist/server.js",
    "dev": "NODE_PATH=. nodemon ./dist/server.js",
    "build": "tsc --build",
    "test": "Error: no test specified && exit 1"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^18.7.14",
    "json-schema-to-ts": "^2.5.5",
    "nodemon": "^2.0.19",
    "typescript": "^4.8.2"
  },
  "dependencies": {
    "@fastify/cors": "^8.1.0",
    "await-exec": "^0.1.2",
    "await-spawn": "^4.0.2",
    "fastify": "^4.5.3"
  }
}
